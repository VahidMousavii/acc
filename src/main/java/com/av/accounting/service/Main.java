/*
package com.av.accounting.service;


import com.av.accounting.DA.AccountDAimpl;
import org.springframework.boot.SpringApplication;

import java.io.IOException;


*/
/**
 * Main class.
 *//*

public class Main {
    // Base URI the Grizzly HTTP server will listen on



    public static void main(String[] args) throws IOException {

        SpringApplication.run(AccountDAimpl.class, args);








       */
/* final ResourceConfig rc = new ResourceConfig().packages("controller");
        final HttpServer server = GrizzlyHttpServerFactory.createHttpServer(URI.create(Phrases.BASE_URI), rc);
        System.out.println(String.format("Jersey app started with WADL available at " + "%sapplication.wadl\nHit enter to stop it...", Phrases.BASE_URI));
        System.in.read();
        server.stop();*//*

    }
}*/
